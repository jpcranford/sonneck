// Phase 0 placeholder — the real sidebar shell + routes land in Phase 1.
// This just proves the toolchain (Tailwind tokens, self-hosted Baskervville,
// TanStack Query, React Router) is wired up correctly.
function App() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-paper text-ink">
      <p className="font-display text-2xl">Sonneck</p>
    </div>
  )
}

export default App
