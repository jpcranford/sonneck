import { ComingSoon } from '../components/ComingSoon'

// Phase 3 wires this up to GET /api/pieces (grid/list cards, search-as-you-type).
export function LibraryPage() {
  return <ComingSoon title="Library" />
}
